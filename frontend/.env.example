# Optional. Leave unset for same-origin (Vercel deploy) or the Vite dev proxy.
# Set this only if the API is hosted separately (e.g. Render/Railway):
#   VITE_API_BASE=https://your-backend.example.com
VITE_API_BASE=
